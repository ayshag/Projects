"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var express = require("express");
var graphqlHttp = require("express-graphql");
function startServer(options) {
    var app = express();
    var endpoint = options.endpoint.startsWith('/') ? options.endpoint : "/" + options.endpoint;
    app.use(endpoint, graphqlHttp({
        schema: options.schema,
        rootValue: options.rootData,
        graphiql: true,
    }));
    app.listen(options.port, function () {
        console.log("gql-server listened on http://" + options.host + ":" + options.port + endpoint);
    });
}
exports.startServer = startServer;
//# sourceMappingURL=index.js.map